
services:
  redis:
    image: redis:latest
    container_name: vibe-matcher-redis
    ports:
      - "6379:6379"
    command: redis-server --requirepass "Ramram@1"
    volumes:
      - redis_data:/data

volumes:
  redis_data: