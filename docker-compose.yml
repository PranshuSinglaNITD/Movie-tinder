
services:
  redis:
    image: redis:latest
    container_name: vibe-matcher-redis
    ports:
      - "6379:6379"
    command: redis-server --requirepass "PASSWORD_CLI_REDIS"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
